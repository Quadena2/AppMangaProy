spring.application.name=proyecto
# Server
server.port=${PORT:8080}

# PostgreSQL
spring.datasource.url=jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:tu_base_local}
spring.datasource.username=${PGUSER:tu_usuario_local}
spring.datasource.password=${PGPASSWORD:tu_password_local}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=p12s356364h36sufhsu334572364ddjsd3636
jwt.expiration=86400000

# Google OAuth
google.client.id=802810257346-ad4s6omans1mkgaff1r3dik8vkjj9naq.apps.googleusercontent.com

# Cloudinary
cloudinary.cloud-name=daokenjof
cloudinary.api-key=139865689947315
cloudinary.api-secret=wr2L6cxOmhAk7XqgSUsOSSQf_Zw

# Multipart (subida de archivos)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# BCrypt para encriptar contraseñas
spring.security.user.password.noop=false